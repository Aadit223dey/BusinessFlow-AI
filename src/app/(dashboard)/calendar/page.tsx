import { ComingSoon } from "@/components/shared/coming-soon";

export default function CalendarPage() {
  return (
    <ComingSoon
      moduleName="Interactive Schedule Calendar"
      description="View daily, weekly, and monthly schedule grids with staff availability overlays."
      iconName="calendar"
      sprintBadge="Sprint 5 Release"
    />
  );
}
