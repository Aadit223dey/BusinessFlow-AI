import { ComingSoon } from "@/components/shared/coming-soon";

export default function InventoryPage() {
  return (
    <ComingSoon
      moduleName="Inventory & Supplies"
      description="Track stock levels, reorder alerts, product usage per service, and supplier logs."
      iconName="inventory"
      sprintBadge="Sprint 7 Release"
    />
  );
}
