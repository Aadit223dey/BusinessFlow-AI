import { ComingSoon } from "@/components/shared/coming-soon";

export default function AppointmentsPage() {
  return (
    <ComingSoon
      moduleName="Appointments Engine"
      description="Book, reschedule, cancel, and track customer appointment bookings in real-time."
      iconName="appointments"
      sprintBadge="Sprint 5 Release"
    />
  );
}
