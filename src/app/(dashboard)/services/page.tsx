import { ComingSoon } from "@/components/shared/coming-soon";

export default function ServicesPage() {
  return (
    <ComingSoon
      moduleName="Services Catalog"
      description="Define service packages, pricing structures, durations, and category tags."
      iconName="services"
      sprintBadge="Sprint 6 Release"
    />
  );
}
