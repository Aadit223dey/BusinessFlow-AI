import { ComingSoon } from "@/components/shared/coming-soon";

export default function SettingsPage() {
  return (
    <ComingSoon
      moduleName="Workspace Settings & Configuration"
      description="Configure workspace preferences, branding details, team permissions, and notification channels."
      iconName="settings"
      sprintBadge="Sprint 8 Release"
    />
  );
}
